<div class="survey-container">
  <h1><%= @survey.name %></h1>

  <div class="take-survey-container" >
    <%= form_for :your_answers, url: user_surveys_path do |form| %>

      <%= render @survey.questions %>
      <%= form.submit class: "submit-button" %>
    <% end %>
  </div>
</div>
